[global]
error_log = /proc/self/fd/2
log_limit = 8192

; Emergency restart settings (must be in global section)
emergency_restart_threshold = 10
emergency_restart_interval = 1m

include=/etc/php85/php-fpm.d/*.conf