<?xml version="1.0" encoding="UTF-8"?>
<ruleset name="RSS-Bridge Ruleset">
  <description>
    Modernized ruleset for RSS-Bridge bridges (PHP 8.5+).
    Based on PSR-12 with strict typing and modern PHP practices.
  </description>

  <!-- Specify the target PHP version for the analyzers to work correctly. -->
  <config name="php_version" value="80500"/>

  <!-- Default check paths -->
  <file>./bridges</file>
  <file>./lib</file>
  <file>./actions</file>

  <!-- Exceptions (Legacy code, vendors, and statics only) -->
  <exclude-pattern>./vendor/*</exclude-pattern>
  <exclude-pattern>./static/*</exclude-pattern>
  <exclude-pattern>./templates/*</exclude-pattern>
  <exclude-pattern>./lib/parsedown/*</exclude-pattern>
  <exclude-pattern>./lib/php-urljoin/*</exclude-pattern>
  <exclude-pattern>./lib/simplehtmldom/*</exclude-pattern>
  <exclude-pattern>./config.default.ini.php</exclude-pattern>
  <exclude-pattern>./config.ini.php</exclude-pattern>

  <!-- Basic standard PSR-12 -->
  <rule ref="PSR12">
    <!-- 
      NOTE: The MissingNamespace exception is retained for compatibility with older bridges.
      Ideally, all new bridges should use namespaces (e.g., RssBridge\Bridge\...).
      The MultipleClasses, SideEffects, EndFileNewline, and ConstantVisibility exceptions have been removed,
      as they violate modern code quality standards.
    -->
    <exclude name="PSR1.Classes.ClassDeclaration.MissingNamespace"/>
  </rule>

  <!-- Requiring strong typing in all files -->
  <rule ref="Generic.PHP.RequireStrictTypes"/>

  <!-- Required for PHP 8+: Disable loose comparisons (requires === and !==) -->
  <rule ref="Squiz.Operators.ComparisonOperatorUsage"/>

  <!-- Short array syntax [] -->
  <rule ref="Generic.Arrays.DisallowLongArraySyntax"/>

  <!-- Line length limitation -->
  <rule ref="Generic.Files.LineLength">
    <properties>
      <property name="lineLimit" value="180"/>
      <property name="absoluteLineLimit" value="180"/>
      <property name="ignoreComments" value="true"/>
    </properties>
  </rule>

  <!-- Disabled, deprecated, and specific Features -->
  <rule ref="Generic.PHP.ForbiddenFunctions">
    <properties>
      <property name="forbiddenFunctions" type="array">
        <!-- Aliases and legacy functions -->
        <element key="sizeof" value="count"/>
        <element key="delete" value="unset"/>
        <element key="print" value="echo"/>
        <!-- Deprecated in PHP 8.2+ (use mb_convert_encoding) -->
        <element key="utf8_encode" value="mb_convert_encoding"/>
        <element key="utf8_decode" value="mb_convert_encoding"/>
        <element key="returnClientError" value="throwClientException"/>
        <element key="returnServerError" value="throwServerException"/>
      </property>
    </properties>
  </rule>

  <!-- Code analysis -->
  <rule ref="Generic.Classes.DuplicateClassName"/>
  <rule ref="Generic.CodeAnalysis.UnconditionalIfStatement"/>
  <rule ref="Generic.CodeAnalysis.UnnecessaryFinalModifier"/>
  <rule ref="Generic.CodeAnalysis.UselessOverridingMethod"/>
  <rule ref="Generic.Files.ByteOrderMark"/>
  <rule ref="Generic.Functions.FunctionCallArgumentSpacing"/>
  <rule ref="Generic.NamingConventions.UpperCaseConstantName"/>
  <rule ref="Generic.PHP.LowerCaseConstant"/>
  <rule ref="Generic.PHP.LowerCaseType"/>
  <rule ref="Generic.Strings.UnnecessaryStringConcat"/>

  <!-- Formatting and whitespaces -->
  <rule ref="Squiz.WhiteSpace.FunctionOpeningBraceSpace"/>
  <rule ref="Squiz.Strings.ConcatenationSpacing">
    <properties>
      <property name="spacing" value="1"/>
      <property name="ignoreNewlines" value="true"/>
    </properties>
  </rule>
  <rule ref="Squiz.WhiteSpace.CastSpacing"/>
  <rule ref="Squiz.WhiteSpace.OperatorSpacing"/>
  <rule ref="Squiz.WhiteSpace.SemicolonSpacing"/>
  <rule ref="Squiz.WhiteSpace.SuperfluousWhitespace">
    <properties>
      <property name="ignoreBlankLines" value="false"/>
    </properties>
  </rule>

  <!-- Lines -->
  <rule ref="Squiz.Strings.DoubleQuoteUsage">
    <exclude name="Squiz.Strings.DoubleQuoteUsage.ContainsVar"/>
  </rule>

</ruleset>